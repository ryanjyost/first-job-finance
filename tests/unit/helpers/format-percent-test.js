
import { formatPercent } from 'first-job-finance/helpers/format-percent';
import { module, test } from 'qunit';

module('Unit | Helper | format percent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatPercent([42]);
  assert.ok(result);
});

