<div class="card__container row">
  {{#if filterButtons}}
    {{card-filter
      filterButtons=filterButtons
      updateCardList=(action filterCards)
    }}
  {{/if}}


    {{#each filteredCardList as |card|}}
      {{#if (eq cardComponentType "portal")}}
        {{card-portal
          portal=card
        }}
      {{else}}
        {{card-single
          id=card.id
          title=card.title
          description=card.description
          comingSoon=card.comingSoon
          link=card.link
          type=card.type
        }}
      {{/if}}
    {{/each}}


    {{#if (eq cardComponentType "portal")}}
      {{#if addCard}}
        {{!-- Add a portal --}}
        <div class="col-md-4 col-sm-6 col-xs-12 col-centered portal portal--add">
            <div class="portal__inner-cont">
              <input type="text" id="inputPortalName" name="portalName" placeholder="e.g., Chase Checking" required/>
              <input type="text" id="inputPortalLink" name="portalLink" placeholder="paste the website link here" required/>

              <button class="portal__button--add" {{action handleAddPortal}}>
                {{fa-icon "plus"}}
              </button>

            </div>
        </div>
      {{/if}}
    {{/if}}

</div>
