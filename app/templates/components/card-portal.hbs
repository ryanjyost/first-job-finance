<div class="col-md-4 col-sm-6 col-xs-12 col-centered card">

  <a href={{portal.link}} target="_blank" class="card__link"></a>

  <div class="card__inner-cont">
    <a href="" {{action "updateTimeLastVisited" }} >
      <div>
        <h6 class="card__title">
          {{portal.name}}
        </h6>
        <h6 class="card__type">
          Last visited <strong>{{moment-from-now portal.lastVisited}}</strong>
        </h6>

        <button {{action "deletePortal"}} class="card__button--delete">
          {{fa-icon "times"}}
        </button>
      </div>

    </a>
  </div>
</div>