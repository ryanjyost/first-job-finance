<div class="checklist__item row">
    <div class="col-xs-10">
      <h4 class="item__title {{if isComplete "item__title--isComplete"}}">{{item.title}}</h4>
    </div>

    <div class="col-xs-2 checkbox__container">
      {{input type="checkbox" checked=isComplete}}
    </div>
 {{!--    <div class="col-12">
      <p>{{item.description}}</p>
    </div> --}}
</div>
