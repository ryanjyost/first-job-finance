import Ember from 'ember';

export default Ember.Service.extend({
  fedTaxWithholding(annualIncome, allowances, payPeriod){
    //assumes the user is single (not married)
    const bracketTables = [
      {

      }

    ]

  },

});
